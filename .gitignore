# Local env / Python
.venv/
__pycache__/
*.pyc

# Terraform
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
plan.tfout

# DVC-tracked weights (pointer is committed, data is not)
src/models/

# Secrets / logs
*.log
.terraformrc
terraform.rc

# Local test receipt images
test_receipt.png
IMG_*.jpeg
IMG_*.jpg

# Planning artifacts (kept local, not in the repo)
docs/superpowers/
